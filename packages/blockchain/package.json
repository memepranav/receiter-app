{
  "name": "@quran-reciter/blockchain",
  "version": "1.0.0",
  "description": "Solana blockchain integration for Quran Reciter rewards system",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "build-program": "anchor build",
    "deploy": "anchor deploy",
    "test": "anchor test",
    "clean": "rimraf dist && anchor clean"
  },
  "dependencies": {
    "@project-serum/anchor": "^0.26.0",
    "@solana/spl-token": "^0.4.13",
    "@solana/web3.js": "^1.98.4"
  },
  "devDependencies": {
    "rimraf": "^5.0.1",
    "typescript": "^5.1.3"
  }
}