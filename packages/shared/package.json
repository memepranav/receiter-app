{
  "name": "@quran-reciter/shared",
  "version": "1.0.0",
  "description": "Shared types, utilities, and schemas for Quran Reciter platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.1.3",
    "rimraf": "^5.0.1"
  }
}